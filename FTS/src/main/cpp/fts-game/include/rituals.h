#pragma once

// Leave the headers in this file for ease-of-use
#include "spinning_pointer.h"
#include "hand_behavior.h"
#include "moving_sound_box.h"
#include "spawn_box.h"

#define RITUALS(X) \
    X(SpinningPointer) \
    X(HandBehavior) \
    X(MovingSoundBox) \
    X(SpawnBox) \
